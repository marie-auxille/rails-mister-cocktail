<h2>Add a dose</h2>
<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.label :description%>
  <%= f.text_field :description%>
  <%= f.association :ingredient%>
  <%= f.submit %>
<% end %>

<%= link_to "Go back to cocktail list", cocktails_path, class: "btn btn-primary"%>
